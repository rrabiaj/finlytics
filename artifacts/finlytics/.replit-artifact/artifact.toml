kind = "web"
previewPath = "/"
title = "Finlytics"
version = "1.0.0"
id = "artifacts/finlytics"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24971

[services.development]
run = "pnpm --filter @workspace/finlytics run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/finlytics", "run", "build" ]
publicDir = "artifacts/finlytics/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24971"
BASE_PATH = "/"
